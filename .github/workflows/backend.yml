# name: Backend Check
# on: [push]
# jobs: 
#    run-tests:
#        runs-on: ubuntu-latest
#        defaults:
#           run:
#             working-directory: ./BACKEND


name: Backend Check
on: [push]
jobs:
  run-test:
     runs-on: ubuntu-latest
     steps:
       - name: Say Hi
         run: echo "Hi how are you"